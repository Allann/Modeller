# GetSyncJobProgress Query
# Retrieves the progress of a sync job

query GetSyncJobProgress
  "Retrieves the current progress of a background sync job"

  input
    JobId: guid "The unique identifier for the sync job"
  end

  returns SyncJob
end

